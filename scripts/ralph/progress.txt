# Ralph Progress Log
Started: Initial setup
---

## Codebase Patterns
- SQLAlchemy models go in backend/src/models/ and must be imported in __init__.py
- API routes go in backend/src/api/routes/ and must be registered in __init__.py
- Use structlog for logging, never print()
- Frontend API client is at frontend/src/lib/api/client.ts
- Supabase client is at frontend/src/lib/supabase.ts
- Run `uv run ruff check src/` before committing backend changes
- Run `npm run build` before committing frontend changes
- Database uses Supabase PostgreSQL via asyncpg
- Use `Enum(EnumClass, native_enum=False, length=N)` for enum columns, not `String(N)`
- Use TYPE_CHECKING guard for circular import prevention in relationship models

---

## 2026-01-28 - US-002: Create Call database model
- **Subagents used**:
  - architect: Not needed - patterns clear from US-001
  - specialist: backend - created Call model and updated User relationship
  - simplifier: not needed - code straightforward
  - reviewer: Found major issue with enum type (String vs Enum), fixed
- **What was implemented**:
  - Created CallStatus enum with SCHEDULED, IN_PROGRESS, COMPLETED, FAILED values
  - Created Call model with UUID pk, user_id FK (indexed), status, timestamps
  - Added bidirectional relationship between User and Call with back_populates
  - Used proper SQLAlchemy Enum type with native_enum=False for portability
- **Files changed**:
  - backend/src/models/call.py (created)
  - backend/src/models/user.py (added calls relationship)
  - backend/src/models/__init__.py (added Call, CallStatus exports)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Use ondelete="CASCADE" on FK and cascade="all, delete-orphan" on relationship for proper deletion
---

## 2026-01-28 - US-003: Create Transcript database model
- **Subagents used**:
  - architect: Not needed - patterns established from US-001/US-002
  - specialist: Implemented directly following established patterns
  - simplifier: Not needed - code is straightforward
  - reviewer: Approved with minor suggestions (order_by on relationship - implemented)
- **What was implemented**:
  - Created Speaker enum with AGENT, USER values
  - Created Transcript model with UUID pk, call_id FK (indexed), speaker, content (Text), timestamp_ms
  - Added bidirectional relationship between Call and Transcript with back_populates
  - Added order_by="Transcript.timestamp_ms" to Call.transcripts relationship for chronological ordering
  - No updated_at field - transcripts are immutable records
- **Files changed**:
  - backend/src/models/transcript.py (created)
  - backend/src/models/call.py (added transcripts relationship with ordering)
  - backend/src/models/__init__.py (added Transcript, Speaker exports)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Use order_by on relationships where ordering matters (e.g., conversation transcripts)
  - Immutable records (like transcripts) don't need updated_at field
---

## 2026-01-28 - US-004: Create Summary database model
- **Subagents used**:
  - architect: Not needed - patterns well-established
  - specialist: Implemented directly following established patterns
  - simplifier: Not needed - code is straightforward
  - reviewer: Approved with suggestions (added updated_at field since posted_to_slack can change)
- **What was implemented**:
  - Created Summary model with UUID pk, call_id FK (unique for one-to-one), text fields for yesterday/today/blockers/raw_summary
  - Added posted_to_slack (boolean) and slack_message_id (string nullable) for Slack integration
  - Added updated_at field since summary fields can be updated (e.g., posting to Slack)
  - Added one-to-one relationship between Call and Summary using uselist=False
- **Files changed**:
  - backend/src/models/summary.py (created)
  - backend/src/models/call.py (added summary relationship with uselist=False)
  - backend/src/models/__init__.py (added Summary export)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Use uselist=False for one-to-one relationships
  - Use unique=True on FK for one-to-one constraint at DB level
  - Add updated_at to models that can be modified after creation
---

## 2026-01-28 - US-005: Create Schedule database model
- **Subagents used**:
  - architect: Not needed - patterns well-established
  - specialist: Implemented directly following established patterns
  - simplifier: Not needed - code is straightforward
  - reviewer: Approved with suggestions (composite index for scheduler queries - future enhancement)
- **What was implemented**:
  - Created Schedule model with UUID pk, user_id FK (indexed), enabled (boolean), cron_expression, next_run_at
  - Added bidirectional relationship between User and Schedule with back_populates and cascade
  - Users can have multiple schedules (one-to-many relationship)
- **Files changed**:
  - backend/src/models/schedule.py (created)
  - backend/src/models/user.py (added schedules relationship)
  - backend/src/models/__init__.py (added Schedule export, reformatted __all__ for readability)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Consider composite indexes for frequent query patterns (e.g., enabled + next_run_at for scheduler)
  - Cron expression validation should happen at service/API layer, not in model
---

## 2026-01-28 - US-006: Create Alembic migration for all models
- **Subagents used**:
  - architect: Not needed - migration is straightforward following model definitions
  - specialist: Created migration manually (autogenerate requires live DB connection)
  - simplifier: Not needed
  - reviewer: Approved - all tables, FKs, indexes, and cascades are correct
- **What was implemented**:
  - Created initial migration (001_initial) with all 5 tables
  - Tables: users, calls, transcripts, summaries, schedules
  - All foreign keys with ondelete="CASCADE"
  - Indexes on email (unique), user_id columns, call_id columns
  - Proper downgrade function drops tables in reverse dependency order
  - Used native_enum=False for enum portability
- **Files changed**:
  - backend/alembic/versions/001_create_initial_tables.py (created)
  - backend/alembic/env.py (fixed ruff import ordering)
- **Verification**: ruff ✓ | pytest (no tests yet) | alembic upgrade/downgrade (requires DB)
- **Learnings for future iterations**:
  - Drop tables in reverse dependency order in downgrade (child tables first)
  - Use sa.text() for server defaults like gen_random_uuid() and now()
  - native_enum=False avoids need to explicitly drop enum types in downgrade
---

## 2026-01-28 - US-007: Create Pydantic schemas for API
- **Subagents used**:
  - architect: Analyzed Pydantic v2 patterns, model field types, enum imports
  - specialist: backend - created all Pydantic schemas in schemas.py
  - simplifier: Not needed - code is well-structured
  - reviewer: Approved with minor suggestions (cron validator could be more permissive, duplicate validator code)
- **What was implemented**:
  - Created Pydantic v2 schemas: UserCreate/Read/Update, CallCreate/Read/Update, TranscriptRead, SummaryRead, ScheduleCreate/Read/Update
  - Used ConfigDict(from_attributes=True) for ORM compatibility
  - EmailStr for email validation
  - Field constraints matching SQLAlchemy model limits
  - Cron expression validator (5 fields, basic format checking)
  - Imported CallStatus and Speaker enums from src.models
  - Added email-validator>=2.2.0 to dependencies
- **Files changed**:
  - backend/src/api/schemas.py (created)
  - backend/pyproject.toml (added email-validator)
  - backend/uv.lock (updated)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Use ConfigDict(from_attributes=True) for Pydantic v2 ORM mode (not class Config)
  - Pydantic v2 handles UUID and datetime natively
  - Import enums from models to keep single source of truth
  - Consider more comprehensive cron validation with croniter library in future
---

## 2026-01-28 - US-008: Create Users API endpoints
- **Subagents used**:
  - architect: Analyzed existing API patterns, router registration, database session handling
  - specialist: backend - created Users CRUD endpoints
  - simplifier: Not needed - code is well-structured
  - reviewer: Found security suggestions (auth needed - deferred to future story), PII logging issue (fixed)
- **What was implemented**:
  - Created backend/src/api/routes/users.py with all 5 CRUD endpoints
  - GET /users - list with pagination (skip, limit params, max 1000)
  - GET /users/{id} - get single user by UUID
  - POST /users - create with 201 status, 409 on duplicate email
  - PATCH /users/{id} - partial update with model_dump(exclude_unset=True)
  - DELETE /users/{id} - delete with 204 No Content
  - Async database operations with proper error handling
  - Removed PII (email) from log statements for security
  - Added B008 to ruff ignore (FastAPI Depends() pattern)
- **Files changed**:
  - backend/src/api/routes/users.py (created)
  - backend/src/api/routes/__init__.py (registered users router)
  - backend/pyproject.toml (added B008 to ruff ignore)
- **Verification**: ruff ✓ | pytest (no tests yet) | build (backend only)
- **Learnings for future iterations**:
  - Use Query() with ge/le for pagination parameter validation
  - Don't log PII (emails, phone numbers) - log user_id instead
  - B008 rule needs to be ignored for FastAPI Depends() pattern
  - Use model_dump(exclude_unset=True) for PATCH partial updates
  - IntegrityError handling for unique constraint violations
---
